{
  "name": "@strobelabs/example-app-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "lint": "pnpm run lint:sol && pnpm run prettier:check",
    "lint:sol": "forge fmt --check && pnpm solhint {script,src,test}/**/*.sol",
    "prettier:check": "prettier --check \"**/*.{json,md,yml}\" --ignore-path \".prettierignore\"",
    "deploy:core-sepolia": "source .env && forge script --chain sepolia script/StrobeCoreDeployer.s.sol:StrobeCoreDeployer --rpc-url $SEPOLIA_RPC_URL --broadcast -vvvv --slow",
    "deploy:core-anvil": "forge script script/StrobeCoreDeployer.s.sol:StrobeCoreDeployer --fork-url http://localhost:8545 --broadcast"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1"
  },
  "devDependencies": {
    "forge-std": "github:foundry-rs/forge-std#v1.8.1",
    "prettier": "^3.0.0",
    "solhint": "^3.6.2"
  }
}
